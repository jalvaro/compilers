class _id {
	private float a1,a2;
	char a3;
	protected string a4;
	public int a5;
	int a6;
	private bool a7;

	function _id( string _id_p1 )
	{
		string _id_v1;
		int _id_v2;
		bool _id_v3;
		_id_v4 = 1 + 1 + 1 *2+asd()+asd()+asd()+asd()+asd()+asd+asd;
	}
	function _id2( bool _id2_p1, char _id2_p2 )
	{
		string _id2_v1;
		int _id2_v2;
		bool _id2_v3;
	}
	function bool iden2( float _iden2_p1, int _iden2_p2, int _iden2_p3 )
	{
		string a8;
		int a9;
		bool a10;

		if(simple){
			simple = true;
		}

		if(simple){
			simple = true;
		} else {
			simple = false;
		}

		if(simple){
			simple = true;
		} elseif (!simple) {
			simple = !simple;
		}

		if(simple){
			simple = true;
		} elseif (!simple) {
			simple = !simple;
		} else {
			simple = false;
		}
		
	}
	function int iden3( bool _iden3_p1, bool _iden3_p2 )
	{
		string cole, hola;
		strange = (as== !sd || !asd);

		notSoSimple = h==As && !!!(as== !!!!!!sd || !asd) || (no!=yes);

		if(simple){
			simple = true;
		}

		if(simple){
			simple = true;
		}

		if (h==As && (as== !sd || !asd) || (no!=yes)) {
			wooo = asd + 3*(casca && identifier5(jsjsj, asdsa, asdas));
		}
		
		if (h==As && (as== !sd || !asd) || (no!=yes)) {
			foo = !!identifier5(jsjsj, asdsa, asdas);
		} else {
			qwe = 1 ++ 1 + 1*2+asd()+asd()+asd()+asd()+asd()+asd+asd;
		}

		if (h==As && (as== !sd || !asd) || (no!=yes)) {
			noo = identifier5(jsjsj, asdsa, asdas);
		} elseif (+1.3 - 3.2 < 12.3222 *21312) {
			really = asd+(asd||asd&&(asd/123/231*call()*asdas(asda,qweqw))) >= 1.0;
		}

		if (h==As && (as== !sd || !asd) || (no!=yes)) {
			again = (haha != identifier5(jsjsj, asdsa, asdas)) * 3 + 3;
		} elseif (asd+(asd|| !!(asd) &&( (!asd)/123/231*call()*asdas(asda,qweqw))) >= 1.0) {
			asd = qwe * (123+true&&(casa||peix));
			asd = qwe * (123+ !true &&(casa||peix));
		} else {
			asd = qwe * (123+true&&(casa|| !peix));
			asd = qwe * (123+true&&(casa|| !!!peix));
		}
	}
	function int iden4(  )
	{
		asd = call();
	}
	function int iden5(  )
	{
		asd = call();
		asd = call(ard, qwe, oier);
		asd = qwe * (123+true);
		qwe = !asdas;
		asd = qwe * (123+true&&(casa||peix));
		asd = qwe * (123+true&&(casa||peix));
		asd = qwe * (123+true&&(casa||peix));
		asd = qwe * (123+true&&(casa||peix- 123.00*call(ard, qwe, oier)));
		asd = asd+(asd||asd&&(asd/123/231*call()*asdas(asda,qweqw)));
	}

}